// Placeholder until task 14 builds the real cook-log dialog.
export function LogDialog({ slug }: { slug: string }) {
  return (
    <button type="button" className="rounded bg-stone-100 px-3 py-1 text-sm text-stone-700">
      Log a cook
    </button>
  )
}
